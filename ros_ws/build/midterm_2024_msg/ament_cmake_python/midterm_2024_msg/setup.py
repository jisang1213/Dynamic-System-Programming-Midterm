from setuptools import find_packages
from setuptools import setup

setup(
    name='midterm_2024_msg',
    version='0.0.0',
    packages=find_packages(
        include=('midterm_2024_msg', 'midterm_2024_msg.*')),
)
